# Self-Taught Edgar Crawling Example

Install environment `edgar` from `enrivonment.yml` file:

```bash
conda env create -f environment.yml
```





> Backup purpose:
>
> - Update `environment.yml` with:
>
>   ```shell
>   conda env export > environment.yml
>   ```

